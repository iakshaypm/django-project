from django.apps import AppConfig

class IntegrationConfig(AppConfig):
    """
    IntegrationConfig is a Django application configuration class for the 'integrations'.
    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "integrations"